# AGENTS.md — Watermelon

Repo: `program-health-console` (`shashwatc12/watermelon`). Public entry point: `bin/watermelon.js`.

## Status

Filed as a **System 1** lab (fast lane, per the Multiverse Brand System) — SHI-117 in the
Portfolio & AI Multiverse Hub project. Not full Lab Contract scope, not portfolio evidence
unless promoted to System 2. See that issue for the promotion path.

## Linear continuity

Before creating any Linear document or issue, search for an existing one on the same topic —
do not create a parallel one. Write a status field only as a claim plus its evidence (command
output, file path, or URL produced this session); a claim with no evidence is written as
"unverified," never as a checkmark. (AI SDLC Learning Ledger, 2026-09-21/22.)

Material milestones (a public deploy, a scope change) get a one-line update on SHI-117, not a
new issue. Do not create a dedicated Linear project for this repo without explicit approval —
System 1 labs are declared, not fully onboarded.
